# Core
numpy
Pillow
requests

# Capture
opencv-python          # USB webcam source

# AI inference (tflite-runtime is not published for Windows;
# full TensorFlow provides the same interpreter)
tensorflow

# Dashboard
fastapi
uvicorn
python-multipart

# Raspberry Pi only (uncomment when deploying to the trap hardware)
# gpiozero
# picamera2
